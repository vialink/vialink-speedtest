// Local per-installation configuration — EXAMPLE.
//
// Copy this file to vlk-config-local.js (same directory) and set your values.
// vlk-config-local.js is NOT versioned (.gitignore), so upgrades — git pull,
// CI deploy with `git reset --hard` — never overwrite it. If the file does not
// exist, the defaults from index.html apply (the 404 in the console is harmless).

// Correction factor: displayed download/upload values are the measured values
// multiplied by this factor. Recorded values (optional persistence) stay raw.
// Default: 1.0 (no correction).
// vlkCorrectionFactor = 1.35;
