.DS_Store
.playwright-mcp/
# screenshots de validação só na raiz e em .screenshots/ (PNGs de assets são versionados normalmente)
/*.png
.screenshots/
# arquivo binário gerado pelo CI/CD (100 MB); não versionar
downloading

# log de sessão local (hook), não versionar
.claude/progress.log
